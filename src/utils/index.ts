/**
 * @packageDocumentation
 *
 * Shared validation helpers used across the package.
 *
 * These helpers are reusable, but they exist mainly to support internal
 * validation of values and payload shapes used by the higher-level modules in
 * this package.
 */
export * from './validation'
